node_modules
node_modules*
dist
*.tsbuildinfo
.env
.env.local
.env.*.local
seed/generated
.vercel
test-results
playwright-report
# .mcp.json / .cursor/mcp.json are COMMITTED — the tokenless VVibe MCP config the
# fork needs to connect. Only the *.local.json personal-token overrides stay ignored.
.mcp.local.json
.cursor/mcp.local.json

# Local agent files
.agent
.agents/*
!.agents/skills/
.augment
.claude/*
!.claude/skills/
.cline
.github/copilot*
.kilocode
.qoder
.qwen
.roo
.trae
.windsurf
